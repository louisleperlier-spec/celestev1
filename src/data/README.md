# Données NÉA

Données statiques extraites du prototype `nea-app.html` (cahier des charges, section 5) :

- `coaches.ts` ← `COACHES` (6 coachs)
- `programs.ts` ← `PROGS` (18 programmes)
- `exercises.ts` ← `EXL` (50 exercices)
- `sessions.ts` ← `CAT` (41 séances prêtes)

Les illustrations (mascottes, exercices) vont dans `/assets/images/`.

> ⚠️ En attente : le prototype `nea-app.html` n'était pas disponible lors de l'étape 1.
> Ces fichiers seront générés dès qu'il sera ajouté à `docs/`.
